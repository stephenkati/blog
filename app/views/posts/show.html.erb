<div class="flex justify-center w-full p-4">

  <div class="flex flex-col w-full max-w-md gap-4">

    <div class="border-2 p-2">
      
      <div class="flex justify-between">
        <h1 class="font-bold"> <%= @post.title %> </h1>

        <div class="flex gap-2">
          <p>Comments: <%= @post.comments_counter %> </p>
          <p>Likes: <%= @post.likes_counter %> </p>
        </div>
      </div>

      <p class="p-4" > <%= @post.text %> </p>

    </div>

    <div class="w-full flex gap-2">
      <%= link_to "Add Comment", new_user_post_comment_path(@post.author.id, @post.id), class: "text-white hover:bg-blue-950 bg-blue-500 rounded-full py-2 px-4 text-center" %>
      <%= form_with url: user_post_likes_path(@post.author.id, @post.id) do |form| %>
        <%= form.submit 'Like', class: "text-white hover:bg-blue-950 bg-blue-500 rounded-full py-2 px-4 text-center" %>
      <% end %>
    </div>

    <% if @comments.present? %>
      <%= render "shared/comment", comments: @comments %>
    <% end %>

  </div>

</div>
